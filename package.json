{
  "name": "designlab-v2",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Stage-gated app design tournament with immutable design-to-code handoff for Claude Code.",
  "scripts": {
    "designlab": "node src/cli.mjs",
    "check": "node --check src/cli.mjs && node --check src/pipeline.mjs && node --check src/prompts.mjs && node --check src/design-lock.mjs && node --check src/scanner.mjs && node --check src/claude.mjs"
  },
  "engines": { "node": ">=20" }
}
